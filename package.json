{
  "name": "afterimage-app",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "npm --workspace backend test",
    "typecheck": "npm --workspace backend run typecheck",
    "check:ios": "node scripts/verify-ios-contract.mjs && node scripts/verify-ios-localizations.mjs",
    "check": "npm run check:ios && npm run test && npm run typecheck && npm --workspace backend run deploy:dry"
  }
}
